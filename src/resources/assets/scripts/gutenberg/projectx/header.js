export class ProjectXHeader {
    constructor() {
        const headers = document.querySelectorAll('[data-block="projectx-header"]')
    
        if(headers.length <= 0) return

        headers.forEach((header) => {
           // ... //
        })
    }

    handleHamburger() {
        // ... //
    }
}